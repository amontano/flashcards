<h1>New card</h1>

<% form_for(:card, :url => deck_cards_path(@deck)) do |f| %>
<%= render :partial => 'form', :locals => {:f => f} %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to "Back to #{@deck.title}", deck_path(@deck) %>